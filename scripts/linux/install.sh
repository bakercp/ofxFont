#!/usr/bin/env bash
set -e

#sudo apt-get -qq install -y cmake libblas-dev liblapack-dev libpng-dev libcairo2-dev libjpeg-dev libgif-dev > /dev/null
echo "🤷 Nothing to do for linux64. Moving on ..."
